<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dww.com.demo.depart">

    <application
        android:name="dww.com.demo.depart.DepartApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/depart_app_name"
        android:supportsRtl="true"
        android:theme="@style/common_AppTheme">
        <activity android:name="dww.com.demo.depart.ui.DepartMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>